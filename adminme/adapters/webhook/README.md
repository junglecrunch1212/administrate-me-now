# Webhook adapters (L1)

Generic inbound-webhook translators: iOS Shortcuts drops, Gmail Pub/Sub push
endpoints, third-party webhooks. Each registers an HTTP endpoint and emits
typed events on receipt.

Filled in by prompt 11 and the iOS Shortcuts / Gmail prompts.
