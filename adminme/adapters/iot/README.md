# IoT adapters (L1)

IoT / home-automation translators: Home Assistant bridge (primary).
Ambient-signal sources feed both the Automation product's household-status
view and (where applicable) the event log as context for pipelines.

Filled in by prompt 11 and the Home Assistant prompt.
