# Telephony adapters (L1)

Voice/SMS source translators: Twilio, RingCentral. Each emits `telephony.*`
events that feed both `interactions` and `parties`.

Filled in by prompt 11 and provider-specific prompts.
