# Contacts adapters (L1)

External contact-directory translators: Google People, Apple Contacts
(CardDAV), Microsoft Graph People. Each emits `contacts.*` events feeding
the `parties` projection (SYSTEM_INVARIANTS.md §3).

Filled in by prompt 11 and provider-specific prompts.
