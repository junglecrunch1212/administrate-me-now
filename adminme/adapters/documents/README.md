# Documents adapters (L1)

External document-source translators: Google Drive, OneDrive, local-filesystem
watchers (for document drop folders). Each emits `documents.*` events feeding
the `artifacts` projection (SYSTEM_INVARIANTS.md §3 invariant 6).

Filled in by prompt 11 and provider-specific prompts.
