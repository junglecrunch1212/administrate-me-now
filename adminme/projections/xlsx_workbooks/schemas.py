"""
xlsx workbook per-sheet schemas.

Implemented in prompt 07 per ADMINISTRATEME_BUILD.md §3.11.

Declares which sheet exists in which workbook (`adminme-ops.xlsx`,
`adminme-finance.xlsx`), what columns each sheet has, which columns are
`[derived]` (dropped on reverse), and which columns are Plaid-protected.

Do not implement in this scaffolding prompt. Prompt 07 will fill in.
"""
