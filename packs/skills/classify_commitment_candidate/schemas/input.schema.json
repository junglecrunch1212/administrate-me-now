{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "classify_commitment_candidate input",
  "type": "object",
  "required": ["message_text", "sender_party_id", "receiving_member_id", "thread_context"],
  "additionalProperties": false,
  "properties": {
    "message_text": {
      "type": "string",
      "description": "The inbound message body. May be empty after trimming, but the field is required."
    },
    "sender_party_id": {
      "type": "string",
      "description": "Party id of the message sender, resolved upstream by find_party_by_identifier."
    },
    "receiving_member_id": {
      "type": "string",
      "description": "Member id of the household member the message was routed to."
    },
    "thread_context": {
      "type": "array",
      "description": "Prior turns in the thread, oldest first. Empty array if no prior thread.",
      "items": {
        "type": "object",
        "required": ["role", "text"],
        "additionalProperties": false,
        "properties": {
          "role": { "type": "string" },
          "text": { "type": "string" }
        }
      }
    }
  }
}
